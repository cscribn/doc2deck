# Requirements

This document is the authoritative functional specification. [README.md](README.md) is the operator guide (build, run, and environment examples).

---

## 1. Project Overview & Architecture

### 1.1 Objective

A Java command-line application that automates the generation of context-rich, professionally formatted presentations by mapping detailed text from a DOCX document onto the structured, templated slides of a PPTX deck using Gemini.

### 1.2 Tech Stack Requirements

- **JDK 21 (LTS)**: Java runtime for main program logic and output formatting.
- **Gradle**: Use the Gradle Wrapper (`./gradlew`) for build, run, and test. Call bare `java`, `javac`, or `gradle` only when troubleshooting (for example bootstrapping a missing wrapper or diagnosing toolchain resolution).
- **Runtime availability**: JDK 21 (Eclipse Temurin / Adoptium) is declared via Gradle Java toolchains in `build.gradle.kts`. The Foojay toolchain resolver in `settings.gradle.kts` auto-downloads a matching JDK when one is not installed locally. Confirm toolchain resolution with `./gradlew -q javaToolchains`.

### 1.3 Input & Output Artifacts

1. **Source PPTX (`source.pptx`):** Serves as both the visual baseline template (master layouts, fonts, color themes) and structural instruction deck (containing placeholder layouts or notes detailing what content goes where).
2. **Source DOCX (`source.docx`):** Contains the comprehensive narrative data, detailed descriptions, and foundational facts regarding the subject matter.
3. **Output PPTX (`final_presentation.pptx`):** The fully realized presentation reflecting the design rules, constraints, and synthesized content generated by Gemini.

---

## 2. Core Workflow Engine

The Java application must process files using a strict serial pipeline:

1. **Extraction Phase:** Parse `source.pptx` to identify layout types, slide indexes, custom shapes, and instructions. Parse `source.docx` to extract text blocks, narrative structure, and data elements.
2. **Payload Preparation:** Construct an orchestrated LLM prompt matching the system guidelines below, merging the presentation blueprint, structural context, and source domain knowledge.
3. **Gemini Interaction:** Dispatch the payload via the Gemini client. Request a structured JSON response specifying slide allocations, content replacements, and structural mapping directives.
4. **Rehydration Phase:** Read the structured JSON payload inside the Java layer, clone/modify the template layers derived from `source.pptx`, explicitly preserve style rules, and output the final `.pptx` file.

---

## 3. Gemini System Prompt & Constraints

The application must inject the following system instructions into the prompt payload sent to Gemini. These boundaries govern how Gemini handles presentation structure and content generation:

### 3.1 Slideshow Creation

- **Content Density:** Limit each slide to a single, clear core concept. Use no more than 3-4 bullet points per slide to ensure high scannability.
- **Typography & Hierarchy:** Maintain a strict two-font maximum (one for headers, one for body text). Header text must be at least 32pt and body text at least 18pt to guarantee readability.
- **Visual-to-Text Ratio:** Prioritize high-quality data visualizations, diagrams, or structural layouts over dense walls of text. Avoid bulleted lists if the information can be communicated via a table or process flow.
- **Color Contrast:** Ensure a high contrast ratio between text and background elements. Use a maximum of three colors from the established theme (one dominant, one supporting, one accent).
- **Structure:** Always follow a logical narrative flow, unless otherwise specified:
  - Title/Hook
  - Context or Problem Statement
  - Core Deliverables or Data points
  - Next Steps or Actionable Conclusion
- **Data Presentation:** Label all chart axes clearly. Round numbers to the nearest whole unit or single decimal place unless strict technical precision is explicitly required.
- **Review Guard:** Before finalizing, verify that every slide passes the "squint test" - the visual layout, focal point, and hierarchy should remain distinct even when blurry.
